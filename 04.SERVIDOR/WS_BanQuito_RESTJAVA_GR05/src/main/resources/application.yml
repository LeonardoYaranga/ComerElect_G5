spring:
  datasource:
    # CAMBIO 1: Protocolo mysql y parámetros de seguridad para la nube
    url: jdbc:mysql://34.135.112.222:3306/banquito_db?allowPublicKeyRetrieval=true&useSSL=false
    username: grupo-05
    password: BaRiYa123.
    # CAMBIO 2: El driver ahora es el de MySQL
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  sql:
    init:
      mode: always

server:
  port: 8080